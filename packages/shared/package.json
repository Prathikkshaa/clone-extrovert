{
  "name": "@extrovertai/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types, enums, and constants used by every ExtrovertAI app.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  }
}
