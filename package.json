{
  "name": "extrovertai",
  "version": "0.0.0",
  "private": true,
  "description": "ExtrovertAI — email-first B2B sales outreach platform (monorepo root).",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w @extrovertai/shared && npm run build -w @extrovertai/server && npm run build -w api && npm run build -w worker && npm run build -w web",
    "build:shared": "npm run build -w @extrovertai/shared",
    "build:server": "npm run build -w @extrovertai/server",
    "lint": "eslint .",
    "dev:web": "npm run start -w web",
    "dev:api": "npm run dev -w api",
    "dev:worker": "npm run dev -w worker"
  },
  "devDependencies": {
    "@eslint/js": "^9.18.0",
    "@types/node": "^24.0.0",
    "eslint": "^9.18.0",
    "supabase": "^2.116.0",
    "typescript": "~6.0.2",
    "typescript-eslint": "^8.20.0"
  }
}
