// Template for environment.ts (which is generated from the repo-root .env by
// scripts/gen-web-env.mjs and is gitignored). These are PUBLIC client values —
// never put the service_role key here.
export const environment = {
  production: false,
  appName: 'ExtrovertAI',
  signupCredits: 100,
  supabaseUrl: '',
  supabaseAnonKey: '',
  apiUrl: 'http://localhost:3000',
  devAuthBypass: false,
};
